interface PartnerCardProps {
  name: string;
  logoSrc?: string;
  description?: string;
  url: string;
}

export default function PartnerCard({ name, logoSrc, description, url }: PartnerCardProps) {
  return null;
}
